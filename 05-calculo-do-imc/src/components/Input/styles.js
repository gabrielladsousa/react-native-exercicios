import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  input:{
    height: 40,
    width: 300,
    borderWidth: 1,
    borderColor: 'black',
    marginTop: 20,
    fontSize: 16,
    padding: 10,
  }
});